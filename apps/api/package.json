{
  "name": "@umc/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(@umc/api) no lint\"",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@umc/types": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.20.0"
  }
}
